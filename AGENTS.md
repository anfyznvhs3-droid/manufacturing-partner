# 제조도메인 AX 스킬레톤 운영 규칙

## 운영 구조

- 제작·운영팀은 2명이다.
- 스킬 사용자는 1명이다.
- 스킬 사용자는 최대 20명의 엔지니어에게서 문서, 구두 인수인계, 시험 기록, 벤더 자료를 받는다.
- 모든 자료는 출처·원본 위치·개정·적용 구성·증거 수준을 보존한 뒤 하나의 `manufacturing-gate/v1` 출력으로 정규화한다.

## 판정 가드레일

- AI의 문서 판정과 실제 운영 권한을 분리한다.
- `ready`만으로 운전·출하·폐기·재작업·공정변경을 승인하지 않는다.
- 출처 충돌, 필수 사실 누락, 합성 핵심 증거, 미확인 구두 주장은 승인 근거로 사용할 수 없다.
- 사실 확인 요청은 해당 자료의 원본 소유자에게만 보낸다.
- 국제표준은 적용 범위와 판정 근거를 선택하는 기준으로만 사용하며, 인증이나 법적 적합성을 주장하지 않는다.

## 출력 언어와 보존

- 운영자가 읽는 규정, 루브릭, 예시, 검수 결과, 설명은 한국어로 작성한다.
- JSON 키, 상태값, 명령어, 경로, 표준 식별자, 제품명, 원본 벤더 문구는 호환성과 증거 보존을 위해 원문을 유지한다.
- 벤더 PDF와 외부 원문은 번역·수정하지 않고 해시와 원본 위치를 보존한다.

## 종료 시 QA

`종료` 또는 `마무리`가 입력되면 모든 테스트와 스킬 검증을 실행하고, 결과를 `my-workflow-skill/` 구조로 정리한다. 실제 사실 정확도와 구성된 회귀 합의율을 구분해 기록한다.
